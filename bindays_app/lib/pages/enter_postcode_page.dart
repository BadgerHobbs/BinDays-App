// External Imports
import 'package:flutter/material.dart';

class EnterPostcodePage extends StatelessWidget {
  const EnterPostcodePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Enter Postcode Page')));
  }
}
